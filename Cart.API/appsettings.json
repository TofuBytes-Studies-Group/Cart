{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",

    // Add the necessary Kafka configs:
    "Kafka": {
        "BootstrapServers": "localhost:9092",
        "GroupId": "groupId",
        "ConsumerTopic": "add.to.cart",
        "ProducerTopic": "create.order"
    },

    "ConnectionStrings": {
        "Redis": "localhost:6379"
    }
}